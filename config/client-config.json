
{
  "disable_remote_exec": true,
  "retry_join": [ "server" ],
  "bind_addr": "'{{ GetAllInterfaces | include \"network\" \"${CONSUL_SUBNET}\" }}'",
  "advertise_addr": "'{{ GetAllInterfaces | include \"network\" \"${CONSUL_SUBNET}\" }}'",
  "client_addr": "0.0.0.0",
  "datacenter": "${CONSUL_DATACENTER}",
  "data_dir": "${CONSUL_DATA_DIR}",
  "node_name": "${NODE_NAME}",
  "addresses": { "http": "${NODE_IP}" }
}
