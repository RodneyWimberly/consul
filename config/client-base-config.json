{
  "disable_remote_exec": true,
  "datacenter": "stable",
  "node_name": "${NODE_NAME}",
  "data_dir": "/consul/data",
  "server": ${NODE_IS_MANAGER},
  "addresses": {
    "http": "${NODE_IP}"
  },
  "retry_join": [
    "server"
  ]
}
